# 开发规范

适用项目：`项目文件/ai-notes-mobile-prototype`

当前前端已从单文件原型拆成可协作框架，基本满足多人开发；后续重点是守住模块边界、接口契约和高风险文件同步机制。

## 1. 核心原则

- 页面只负责渲染和交互，不直接适配后端临时字段。
- 后端返回统一收敛到 `AgentResult`，字段变化先改契约，再改适配层。
- 演示兜底和真实 API 双轨并存，任何时候不能破坏 Demo 链路。
- 高频共用文件少改、集中改、提前说，避免多人同时抢同一个入口。

## 2. 当前框架

```text
App.jsx                         根入口，仅转发到 src/app/App.jsx
src/app/                        应用组合、导航状态、页面切换
src/components/                 通用 UI 组件
src/data/                       演示数据，只服务 demoMode
src/features/ai/                AI 输入、生成、结果页、接口适配
src/features/notes/             笔记列表、笔记详情、复习评估
src/features/mindmap/           思维导图页面
src/features/profile/           个人页
src/services/                   API 客户端、运行模式
docs/                           前端架构、接口契约、协作清单
.env.example                    环境变量样例
```

关键文件：

```text
src/app/App.jsx
src/app/navigation.js
src/features/ai/agentApi.js
src/features/ai/agentTypes.js
src/services/apiClient.js
src/services/demoMode.js
src/data/demoAgentResult.js
docs/agent-result-contract.md
docs/frontend-architecture.md
docs/collaboration-checklist.md
```

## 3. 前端开发规则

- 新页面放进对应 `src/features/*`，不要回到根目录堆代码。
- 通用组件放 `src/components`，业务专用组件留在 feature 内。
- 导航 tab、页面 id、状态常量统一从 `src/app/navigation.js` 取。
- 页面组件不写 `fetch`，请求统一走 `agentApi.js` 或 `src/services`。
- 后端字段变化只在 `agentTypes.js` 做归一化，页面继续消费稳定结构。
- mock/demo 数据只放 `src/data`，不要散落在页面组件里。
- `.env` 不提交；需要新增变量时同步更新 `.env.example`。
- 移动端优先，重点保证 390px 到 430px 宽度可读、可点、不卡死。

## 4. API 契约

复赛版本先以同步接口为准：

```text
POST /api/agent/run
```

前端期望返回接近以下结构：

```json
{
  "topic": "",
  "summary": "",
  "agentStages": [],
  "sources": [],
  "notes": [],
  "citations": [],
  "mindMap": {
    "nodes": [],
    "edges": []
  },
  "review": {
    "questions": [],
    "masteryScore": 0,
    "weakPoints": [],
    "recommendations": []
  }
}
```

规则：

- 契约文档以 `docs/agent-result-contract.md` 为准。
- 字段使用英文 camelCase。
- 已发布字段尽量只新增，不随意改名或删除。
- 可选字段必须有默认值。
- `citations` 必须能定位到 `sources`。
- 模型原始输出由后端清洗，前端不直接消费大模型原文。

## 5. Demo/API 双轨

环境变量：

```text
VITE_DEMO_MODE=true
VITE_API_BASE_URL=http://127.0.0.1:8000
```

规则：

- `VITE_DEMO_MODE=true`：使用 `src/data/demoAgentResult.js`。
- `VITE_DEMO_MODE=false`：调用真实后端。
- API 失败要展示错误状态，不能白屏。
- 真实联调时保留一份可复现返回 JSON，方便前后端对齐。
- 用缓存或演示数据录屏时，材料里要说明真实 API 证据在哪里。

## 6. 高风险文件

以下文件修改前先同步：

```text
src/app/App.jsx
src/app/navigation.js
src/features/ai/agentTypes.js
src/features/ai/agentApi.js
src/services/apiClient.js
src/data/demoAgentResult.js
docs/agent-result-contract.md
```

协作规则：

- 同一时间只保留一个主改人。
- 大文件改动尽快提交，不长期占用。

## 7. Git 规则

建议分支：

```text
feat/agent-pipeline
feat/api-provider
feat/frontend-api-integration
feat/android-webview
docs/submission-materials
```

提交格式：

```text
feat(ai): add agent result adapter
fix(notes): restore citation popover
docs(plan): update collaboration guideline
chore(build): update env example
```

禁止提交：

```text
.env
API Key
模型 Token
node_modules
dist
日志文件
无关大文件
```

一个提交尽量只解决一类问题，不把 Prompt、后端、前端、材料混在一起大改。

## 8. 合并检查

每次合并前确认：

- `npm run build` 通过。
- 首页、AI 输入、生成结果、笔记详情、思维导图、复习评估可打开。
- `VITE_DEMO_MODE=true` 仍可完整跑 Demo。
- 改 API 字段时已更新 `docs/agent-result-contract.md`。
- 改导航或页面状态时已更新 `src/app/navigation.js`。
- 没有提交密钥、日志、构建产物和本地环境文件。
