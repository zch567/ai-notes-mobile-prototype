# 开发规范

适用项目：`项目文件/ai-notes-mobile-prototype`

当前版本已经从单纯前端原型演进为同仓库交付：React/Vite 移动端、Android WebView 壳、FastAPI 后端和联调文档都在本仓库内维护。后续协作重点是守住 `AgentResult` 契约、真实后端链路、运行时后端配置和构建产物边界。

## 1. 核心原则

- 页面只消费归一化后的 `AgentResult`，不要直接依赖模型原始输出或后端临时字段。
- 前端只保留真实后端模式；不要重新加入页面级 mock 兜底。
- 后端字段变化先改 `src/features/ai/agentTypes.js` 和 `docs/agent-result-contract.md`，再改页面。
- 页面组件不直接写 `fetch`；请求逻辑放在 `src/features/ai/agentApi.js` 或 `src/services/`。
- 运行时配置、草稿、笔记编辑和复习进度等本机数据统一走 `src/services/localDemoFs.js` 或对应服务，不要散落访问 `localStorage`。
- 不提交 `.env`、`api`、API Key、模型 Token、`node_modules`、`dist`、`dist-webview`、APK/AAB、`.venv` 和运行日志。

## 2. 当前结构

```text
App.jsx                         根入口，仅转发到 src/app/App.jsx
src/app/                        应用组合、导航状态、页面切换
src/components/                 通用 UI 组件
src/data/                       输入样例数据，不作为生成结果兜底
src/features/ai/                AI 输入、生成、结果页、接口适配
src/features/notes/             笔记列表、笔记详情、复习评估
src/features/mindmap/           思维导图页面
src/features/profile/           个人页、后端连接配置入口
src/services/                   API 客户端、运行时配置、本机存储
docs/                           前端架构、接口契约、联调清单
android-shell/                  Android 原生 WebView 壳
backend/                        FastAPI 后端、Prompt、RAG、Provider、测试
.env.example                    前端环境变量样例
```

后端重点目录：

```text
backend/app/main.py             FastAPI 路由入口
backend/app/service.py          Agent 服务编排入口
backend/app/agents/             M1-M7 Agent 流程
backend/app/prompts/            Prompt 注册与模板
backend/app/providers/          蓝心模型 Provider
backend/app/rag/                解析、分块、检索、引用 grounding
backend/app/contracts.py        后端契约模型
backend/app/normalization.py    AgentResult 归一化
backend/scripts/                辅助脚本，例如依赖自检
backend/tests/                  后端测试
backend/runtime/                运行产物，禁止提交
```

## 3. 前端开发规则

- 新页面放进对应 `src/features/*`，不要回到根目录堆代码。
- 通用 UI 放 `src/components`，业务专用组件留在 feature 内。
- 导航 tab、页面 id、状态常量统一从 `src/app/navigation.js` 取。
- AI 生成、文件上传、结果刷新和继续问答统一走 `src/features/ai/agentApi.js`。
- FormData、JSON 请求和运行时后端地址解析统一走 `src/services/apiClient.js` 与 `src/services/runtimeConfig.js`。
- 后端连接配置入口在“我的 > 后端连接”，保存后会覆盖 `.env` 中的默认 `VITE_API_BASE_URL`。
- 移动端优先，重点保证 390px 到 430px 宽度可读、可点、不卡死。
- Android WebView 模式下注意安全区、横屏导图、局域网 HTTP 访问和资源相对路径。

## 4. 后端开发规则

- 后端启动入口是 `backend/run.py`。它会先检查当前 Python 环境是否满足 `requirements.txt`，缺失依赖会自动安装。
- `.env` 放在 `backend/.env`，可从 `backend/.env.example` 复制；真实密钥不要提交。
- 当前模型 Provider 只保留 `lanxin`，没有 mock provider 兜底。
- Prompt 放在 `backend/app/prompts/`，修改 Prompt 时同步关注输出 JSON 是否仍能归一化为 `AgentResult`。
- 引用不要交给模型自由编造；优先通过 RAG 检索和 grounding 生成 `sources`、`citations`、`notes[].citationIds`。
- 后端新增字段应保持可选扩展，避免破坏已有前端页面。
- 后端运行产物默认进入 `backend/runtime/`，不要把上传文件、输出 JSON、日志当代码提交。

## 5. API 契约

当前前端已接入的真实接口：

```text
GET  /health
GET  /api/providers/status
POST /api/agent/run
POST /api/agent/run-file
GET  /api/agent/result/{result_id}
POST /api/agent/chat
POST /api/agent/validate
POST /api/rag/query
```

规则：

- 详细字段以 `docs/agent-result-contract.md` 为准。
- 联调说明以 `docs/backend-api-interface.md` 为准。
- 首次文本生成走 `POST /api/agent/run`。
- 文件上传走 `POST /api/agent/run-file`，请求类型为 `multipart/form-data`。
- 结果页刷新走 `GET /api/agent/result/{result_id}`。
- 结果页继续问答走 `POST /api/agent/chat`。
- 推荐后端直接返回完整 `AgentResult`，必要时前端可兼容 `{ "data": AgentResult }`。
- 字段使用英文 camelCase；已发布字段尽量只新增，不随意改名或删除。
- `citations[].sourceId`、`notes[].citationIds`、`review.questions[].citationIds` 必须能定位到 `sources[].id`。

## 6. 运行与构建

前端本地运行：

```powershell
npm install
copy .env.example .env
npm run dev
```

后端本地运行：

```powershell
cd backend
python -m venv .venv
.\.venv\Scripts\activate
copy .env.example .env
# 编辑 .env，填入 LANXIN_API_KEY
python run.py
```

`python run.py` 会自动检查并补齐后端依赖，首次运行可能稍慢。

前端默认真实后端配置：

```text
VITE_DEMO_MODE=false
VITE_API_BASE_URL=http://127.0.0.1:8000
```

APK 访问电脑后端时，后端 `.env` 保持：

```text
BACKEND_HOST=0.0.0.0
BACKEND_CORS_ORIGINS=*
```

手机 App 中填写电脑局域网 IPv4，例如 `http://192.168.1.23:8000`。Android 模拟器访问宿主机可使用 `http://10.0.2.2:8000`。

构建检查：

```powershell
npm run build
npm run build:webview
cd android-shell
.\gradlew.bat assembleDebug --no-daemon
```

Android 构建需要 JDK 17 或更高版本。APK 产物位于 `android-shell/app/build/outputs/apk/debug/app-debug.apk`，不要提交。

## 7. 高风险文件

以下文件修改前要先确认当前任务范围，避免多人冲突：

```text
src/app/App.jsx
src/app/navigation.js
src/features/ai/agentTypes.js
src/features/ai/agentApi.js
src/services/apiClient.js
src/services/runtimeConfig.js
src/services/localDemoFs.js
docs/agent-result-contract.md
docs/backend-api-interface.md
backend/app/main.py
backend/app/contracts.py
backend/app/normalization.py
backend/app/provider_config.py
backend/app/prompts/registry.py
backend/app/agents/orchestrator.py
```

协作规则：

- 同一时间只保留一个主改人。
- 大文件改动尽快提交，不长期占用。
- 如果必须改接口字段，代码、契约文档和联调文档一起更新。
- 如果改运行方式，README、后端 README 和本规范一起更新。

## 8. Git 规则

建议分支：

```text
feat/frontend-flow
feat/backend-agent
feat/api-integration
feat/android-webview
docs/submission-materials
```

提交格式：

```text
feat(ai): add file upload integration
fix(backend): load local env defaults
docs(dev): update development guideline
chore(build): tune webview build output
```

禁止提交：

```text
.env
backend/api
API Key
模型 Token
node_modules
dist
dist-webview
backend/.venv
backend/runtime
*.apk
*.aab
日志文件
无关大文件
```

一个提交尽量只解决一类问题。不要把 Prompt、后端、前端、材料和构建产物混在一个大改里。

## 9. 合并检查

每次合并前确认：

- `git status` 干净，且没有误提交密钥、日志、构建产物和本地环境文件。
- 前端改动至少跑 `npm run build`。
- 后端改动至少跑：

```powershell
cd backend
.\.venv\Scripts\python.exe -m pytest -q -p no:cacheprovider
```

- APK/WebView 改动至少跑 `npm run build:webview`，必要时再跑 Android debug 构建。
- 首页、AI 输入、生成结果、笔记详情、思维导图、复习评估和“我的 > 后端连接”能打开。
- 改 API 字段时已更新 `docs/agent-result-contract.md` 和 `docs/backend-api-interface.md`。
- 改导航或页面状态时已更新 `src/app/navigation.js`。
- 改 Prompt 或后端生成结构时，确认 `normalizeAgentResult` 仍能输出稳定页面数据。
