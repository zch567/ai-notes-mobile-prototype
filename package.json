{
  "name": "ai-notes-mobile-prototype",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node --test src/features/ai/agentTypes.test.js",
    "build": "vite build && node scripts/check_no_secret_leak.mjs dist",
    "build:webview": "vite build --mode webview && node scripts/check_no_secret_leak.mjs dist-webview",
    "check:secrets": "node scripts/check_no_secret_leak.mjs dist dist-webview",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.13",
    "vite": "^5.4.10"
  }
}
